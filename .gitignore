# Build outputs and run artifacts. Leaving these untracked makes every build
# report a modified tree, which hides the staleness check that matters.
/multivenue
/*.test
/logs/
/logs.tar.xz
/.cache/
/bin/
