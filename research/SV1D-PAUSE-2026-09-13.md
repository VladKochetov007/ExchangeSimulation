# SV1D pause / exact resumption handoff

Owner requested a graceful stop. No campaign or test process remains from this
turn. Do not resume work until the owner requests it. Do not reset this worktree.

## Goal and boundary

The scientific goal is unchanged: a defensible V2 integrated ecology candidate,
then explicit freeze authorization, untouched holdout validation and autopsy.
R2 remains NON-VIABLE at the 24-hour survival gate; the finite CDF supplier/SV1D
successor is separate. The immediate task is only the capacity/review workflow
repair, not new economics, serialization design, optimization or broad bug hunting.

Current ordering is source review -> pinned Go1.27 build -> independent verified
fresh signed bundle -> seed977 three-arm five-minute finite-cgroup capacity ->
independent capacity verification -> seed659 five-minute activation if authorized
-> activation review -> registered development. No old dev607 sequence. Holdouts
619/631/641 remain untouched until separate explicit freeze authorization.

## Git / accepted work versus unfinished work

Branch: autoresearch/ffa-ecology-gen0. HEAD:
`94e63d6abaaac5d790a070f303a08da4445b28af`, tree
`91183793f1af70e81f9398ff6fce938de82896df`. Pushed commits this turn:

- `67bba66`: model-neutral operational amendment, stable review target,
  pre-start finite-cgroup checks, diagnostic/exit preservation, verified-only
  no-overwrite final capacity publication. Full clean mechanical gates passed.
- `94e63d6`: retained tool paths and pre-start capacity seed/identity allowlist,
  production-argument/predicate regressions and closure note. Full clean make
  test, vet, targeted race, adapter race, live finite-cgroup and diff gates passed.

Neither commit is source-accepted for execution. Historical ef839a2 acceptance
and bundle cannot authorize either or the next correction.

Uncommitted C3 edits at pause:

- analysis/sv1d_capacity.go: strict metadata command/retention fields, all four
  market-data status hashes required and checked against retained bytes; renderer
  report conforms to real producer schema, with overflow-checked frame total.
- analysis/sv1d_capacity_test.go: matching fixtures, real exported renderer type,
  distinct artifact bytes so digest swaps cannot hide in equal fixtures.
- analysis/sv1d_capacity_runner_test.go: stronger tests execute actual metadata
  jq serialization for all arms, not just verifier-shaped metadata structs.
- new analysis/sv1d_capacity_serialization_test.go: actual status production,
  all retained market-data hash mutations, real renderer serialization through
  full renderer verification, required-field/null/unknown/overflow controls.
- activation-contract amendment: prospective owner restriction to Luna xhigh
  ceiling, prohibiting Astra; mandatory independent review remains.
- this handoff and the latest RESUME-HERE pointer.

Latest focused check passed:
`GOMAXPROCS=2 GOMEMLIMIT=4GiB go test ./analysis -run SV1D -count=1`.
Log: `/home/vlad/external-scratch/sv1d-capacity-c2-20260913/controller-sv1d-c3-focused.log`.
Full C3 make test/vet/race/clean-tree checks have NOT run. Do not commit code
until the full required gates pass. Review/refine unfinished tests as needed;
especially preserve positive real-producer checks and malformed/tamper negatives.

## Independent reviews: substantive findings, not availability errors

Fresh native reviewer context was `01a09ae9-3a99-7c91-b12f-54f4c5e9f956`.
It completed actual reviews with independent reproductions:

1. C67 REJECT: temporary tool paths disagreed with retained-path verifier;
   private entry did not enforce capacity seed before execution. Fixed by C2.
2. C2 REJECT: those two fixes accepted, but actual serializers mismatched strict
   verifier schemas for metadata (command/raw_log_policy), status (four market
   data hashes), and renderer (invented required stream_frames). C3 edits above
   address this, but are not yet independently accepted.

Reports, statuses, raw repros, all supplied tests and rejected immutable checkouts:

- `/home/vlad/external-scratch/sv1d-review-repair-20260913/`
- `/home/vlad/external-scratch/sv1d-capacity-c2-20260913/`

C2 final report SHA256:
`c8c081e16ad648ece1b52fbef89f77faba07acb6caf90986dca5e087e82045ed`.
Do not overwrite these packages or checkouts. Reproductions used JSON-only seed
fixtures, harmless process fixtures and typed/actual serializer output. No world
or reserved evidence was opened. The controller independently reproduced the
reported failures; raw before/after/setup-error logs remain in those directories.

At the subsequent owner instruction, both inherited-model agents were closed:
reviewer above and implementer `01a09b14-8b26-7051-9928-67c6329262f1`.
The implementer was stopped mid-task; its partial edits were preserved and the
controller completed compilation/focused tests. It must not review its work.
Do not resume either agent. Explicitly select `gpt-5.6-luna`, xhigh or lower,
for future agents. The required new Luna reviewer must receive both substantive
rejections and verify every closure; the model restriction is not a reason to
discard adverse findings. If allowed review cannot actually execute, use the
bounded external/human fallback, not an unauthorized larger model or self-approval.

## Next steps on owner resumption

1. Recheck HEAD, dirty files, no cherry-pick, disk/RAM and actual running work.
   Finish/review the narrow C3 correction and tests. Add a prospective C2 finding
   closure note; do not rewrite C/C2 verdicts or economics.
2. Run full gates on a clean identical-tree snapshot before the meaningful code
   commit. Archive tests require clean Git; dirty-tree failure is not a pass.
   Prior external validate.sh recipes show the approach. Commit/push only green.
3. Pin the resulting exact C3 in a NEW standalone checkout with real .git dir.
   Keep all further records outside it. Build scripts currently assume that
   layout; no provenance exclusion list or broad framework is needed.
4. Fresh Luna-xhigh independent source review of the pinned package, bounded30m,
   no short-timeout interruption. Preserve separate execution/verdict statuses.
   Resolve substantive rejections; never fabricate acceptance or reuse ef839a2.
5. After genuine acceptance, registered Go1.27 clean build, independently
   reproduced metadata/hashes/plan and new signed-v1 bundle via existing verifier.
   Then actual seed977 capacity in a fresh namespace, 8GiB cgroup, swap0,
   GOMAXPROCS2/GOMEMLIMIT4GiB. Retain old failed namespaces. Use original floor
   formulas; publish no final attestation on failure. Independently verify result.
6. Only then follow current SV1D activation/development protocol. No holdouts.

Performance branch last checked at b1847ac; no new commits at the latest fetch.
Keep asynchronous checks at natural checkpoints, without merging wholesale.
Disk at last check approximately44GiB free, RAM26GiB available, swap0. Nothing
was deleted to meet a floor. All historical evidence and failed attempts remain.
