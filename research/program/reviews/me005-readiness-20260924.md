# ME-005 source-readiness review — 2026-09-24

Review execution: **COMPLETED**. Reviewer: fresh, read-only **Sol-6 medium**
agent `01a0d0d7-f796-74d2-9bff-ed607f80c986`; no Luna or Astra review was
requested. Exact reviewed source HEAD:
`04b6ff42342aba1dfdac81268f330dc623cb94d1`. The reviewer read the
owner's bounded ME-005 instruction, the current router/simulation and
analysis paths, ME-005 idea, and historical V2-2b result. It did not edit
files, run tests/worlds, inspect holdouts, or review a future candidate.

Verdict: **READY AFTER SPECIFIC FIXES**, not approval to launch ME-005.
The existing two-leg FOK router can be reused for a narrower execution and
completed-cashflow question; both the harness and router currently demand
three venue endpoints. A realizable net-result claim would require a
prospectively specified and tested transfer/unwind/financing convention.

The reviewer identified these independent risks:

- `ExecutableSignals` is a quoted policy-gate count, suppressed while a
  group is in flight or the attempt cap has been reached. It is not the
  public/delivered/executable opportunity denominator. Historic
  `analysis/arbitrage.go` uses an omniscient scan and does not implement the
  exact router lot-depth and integer fee rule.
- Full delivered frontiers bind submitted decisions, but do not by
  themselves attest every no-action decision. Two FOK market legs remain
  non-atomic at venue arrival; complete group, failure, and residual are
  distinct states.
- Completed quote cashflow is not location-aware realized profit. Even a
  globally flat base route shifts ABC and USD between separate venue
  accounts. The simulation specifies neither shared wallet nor transfer.
- The earlier V2-2b three-venue screen observed completed routes without
  changing its sampled residual-edge endpoint. It explicitly requested
  event-time, trade-attributed analysis before a price-discovery claim.

The reviewer required explicit two-venue wiring and complete frontiers;
integer/depth/rounding, stale, rejection, mismatch and per-venue ledger
fixtures; an independently defined opportunity funnel and event-time edge
episode estimator; and a declared economic endpoint. A screen limited to
cashflow plus local inventory may omit closeout **only if it makes no profit
claim**. This nuance is retained in the
[readiness assessment](../ideas/ME-005/readiness-20260924.md). This review
is a source-readiness check, not a code or prospective-protocol acceptance.
